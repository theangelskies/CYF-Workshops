Q: What is the difference between `let` and `const`?

A: {YOUR ANSWER HERE}
let allows you to reassign the variable later, while const creates a variable that cannot be reassigned. Both are block-scoped, but const is used when the value should stay the same, and let is used when the value needs to change.

Remember to commit and push when you're finished!